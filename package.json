{
  "name": "nest-optional-validation-pipes",
  "version": "1.1.0",
  "description": "A NestJS addon that provides validation pipes for the simple data types with the option of optionality.\nE.g. a ParseBoolOptionalPipe that validates & transforms @Query parameters that are then either true, false or undefined/null or throw an error.",
  "scripts": {
    "build": "tsc"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "prepublish": "tsc",
  "keywords": [
    "nest",
    "nestjs",
    "pipes",
    "nestjs-pipes",
    "nest-pipes",
    "optional",
    "optional pipes"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/jonasfeld/nest-optional-validation-pipes.git"
  },
  "author": "Jonas Feld",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "@nestjs/common": "^8.0.0"
  }
}
